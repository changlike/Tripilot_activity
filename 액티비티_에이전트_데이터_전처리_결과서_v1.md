SK 네트웍스 Family AI 27기 : ___팀

# 데이터 전처리 결과서 — 액티비티 에이전트 (v1.6)

| 산출물 단계 | 데이터 전처리 |
|---|---|
| 제출 일자 | 2026. 9. 17. |
| 깃허브 경로 | `C:\skn32_final_project\Tour API data\` (원격 저장소 경로는 팀 확정 후 기입) |
| 작성 팀원 | (본인) |

> SK 네트웍스 Family AI 27기 팀 결과서 양식(`[데이터 전처리] 데이터 전처리 결과서 sample.pdf`)의
> "액티비티 에이전트" 섹션을 채운 결과서. 미매칭 8건 중 5건은 수동 큐레이션으로 확정했고(v1.1~v1.3),
> 나머지 3건은 지금 시점에 TourAPI에서 찾지 못해 **보류 상태로 남기고 추후 추가 서칭 후 수기로
> 채워 넣기로 결정**했다(v1.4). 현재 63/66건(95.5%) 커버리지 기준으로 1차 제출 가능한 상태다.

- 작성일 : 2026-09-17 (세부 개정 이력은 하단 "4. 변경 이력" 참고)
- 대상 코드/데이터 : `C:\skn32_final_project\Tour API data\`
- 작성 팀원 : (본인)

---

## 진행 상태 요약 (TODO)

- [x] 미매칭 8건 수동 큐레이션 — 확정 5건 · 보류 3건으로 1차 종결
- [x] `preprocess_activities.py` 실행해 `activities_clean.csv` 생성 (현재 63건)
- [x] `merge_manual_overrides.py` / `preprocess_activities.py`의 pandas CSV 읽기 버그 수정 (아래 2.1 참고)
- [x] 미해결 3건 표기 변형 재검색(각 3~5개 변형) — 이번 시도로는 전부 0건 또는 개별 매장만 검색됨을 확인
- [x] ~~근사매칭 1건("인사동 고미술거리"→"인사동 문화의 거리") 정확도 재검토~~ — 담당자 확인 결과 "인사동 문화의 거리" 상권 안에 "고미술거리"가 하위 구역으로 포함되는 관계로 확인되어 정상 확정 처리
- [ ] **보류 3건(하모니마트 명동역점 · 현대 아이파크몰 · 롯데면세점 본점) 추가 서칭 후 수기 반영** — 이번 라운드에서 못 찾았을 뿐 영구 제외 아님, 다른 키워드/자료로 재시도 예정
- [ ] 음식점 성격 1건(FD 분류, 명동교자)의 소속 에이전트 경계 규칙을 요식업 에이전트와 협의
- [ ] "세대별 인기관광지" 데이터(6종) 활용 여부 결정 — 현재 수집만 되어 있고 파이프라인 미연동
- [ ] Tripilot 내부 서비스 카테고리 매핑표 확정 후 lclsSystm → 내부 category 재태깅

---

## 1. 전처리 개요

### 1-3. 액티비티 에이전트

- **전처리 목적** : 서울 지역 관광지·액티비티 추천에 쓸 장소 데이터를, 데이터랩의 "외국인 관심
  관광지" 랭킹만으로는 주소·좌표·표준 분류가 없어 그대로 쓸 수 없으므로, 한국관광공사
  TourAPI(KorService2)의 실제 장소 상세정보와 결합해 신뢰 가능한 형태로 생산한다.
- **전처리 범위** : 공공 정형 데이터(데이터랩 CSV, TourAPI JSON 응답) 원천 → 명칭 정규화(영·한글
  분리) → API 매칭(자동 → 실패건 수기) → 결측·중복 필터링 → (예정) 내부 카테고리 재태깅. 비정형
  문서·개인정보 없음.
- **사용 도구** : Python 3.11, pandas, requests, python-dotenv, re, csv; 스크립트
  `extract_popular_places.py` · `tour_api_collect.py` · `manual_lookup.py` ·
  `merge_manual_overrides.py` · `preprocess_activities.py` · `debug_search.py`(진단용)

---

## 2. 원천 데이터 분석

### 2.1 데이터 출처 및 현황 — 액티비티 에이전트

| 출처 | 형태 | 현황 |
|---|---|---|
| 한국관광데이터랩 "외국인 관심 관광지" (영어권) | CSV, 100행 × 3열(구분/관광지명/비율) | 서울 지역 외국인 관심 관광지 랭킹 원본 |
| 한국관광데이터랩 "세대별 인기관광지" (20대~60대이상, 전체 6종) | CSV, 각 30행 내외 | **수집만 완료, 파이프라인 미연동** — 활용 여부 미결정 |
| TourAPI(한국관광공사) KorService2 `searchKeyword2` | REST API(JSON) | 장소 상세(contentid·주소·신 분류체계 lclsSystm1~3) 실시간 조회 |

처리 현황(퍼널) : 원본 100행 → 영/한글 분리 후 고유 장소 **66건** → TourAPI 자동 매칭 **58건 성공 /
8건 실패** → 수동 큐레이션(5건 확정 + 3건 보류) → 결측·중복 필터링 →
**현재 63건** (커버리지 63/66 = 95.5%, 보류 3건은 추가 서칭 후 반영 예정)

| 이슈 유형 | 대상 필드 | 판정 기준 | 처리 방식 | 처리량 / 비고 |
|---|---|---|---|---|
| 명칭 표기 불일치 | english_name / korean_name | 데이터랩 표기와 TourAPI 등록명이 괄호·공백·복합표기(가운뎃점 등)에서 다름 | 원본→괄호제거→괄호만제거→괄호+내용제거→공백제거→가운뎃점 분리 순으로 재검색(`generate_keyword_variants`) | 66건 전체에 적용, 실패 시에만 다음 변형 시도 |
| 결측치(매칭 실패, 자동) | contentid 등 전체 | TourAPI 검색 결과 0건(끝까지 재시도 실패) | 별도 `unmatched_for_manual_review.csv`로 분리, 수기 큐레이션 | **8건** → 수동 큐레이션으로 확정 **5건** + 보류 **3건**(1차 종결, 추가 서칭 예정) |
| 결측치(수동 큐레이션, 검색어 재시도) | contentid 등 | 데이터랩 표기 그대로는 실패했으나 다른 표기(약칭·지점명 변경, 상위 상권명 등)로 `manual_lookup.py` 재검색 시 후보 존재 | 사람이 후보 중 제목·주소 정합성 확인 후 직접 확정 | Hello apM(984585) · 종로귀금속거리(132421) · 리움미술관(130525) · 국립현대미술관 서울(1934593) · **인사동 고미술거리→인사동 문화의 거리(132219, "고미술거리"가 "문화의 거리" 상권 내 하위 구역으로 확인됨)** — **5건** |
| 매칭 불가(보류) | contentid | 표기 변형 3~5종으로 추가 재검색해도 이번 라운드에서는 대상을 못 찾거나 매장 단위로만 쪼개져 등록됨을 확인 | `contentid` 빈칸 유지 + `note`에 재검색 이력·사유 기록, `unmatched_for_manual_review.csv`에 보류 상태로 잔존 — 영구 제외 아니고 추후 다른 키워드/자료로 추가 서칭 예정 | 하모니마트 명동역점(하모니마트/하모니마트 명동/명동역 하모니마트/Harmony Mart 재검색해도 홍대입구점만 존재) · 현대 아이파크몰(용산아이파크몰/아이파크몰 용산점/아이파크백화점 재검색해도 나이키·다이소 등 개별 매장만 존재 — 몰 건물 자체가 단일 콘텐츠로 미등록되는 구조적 한계로 추정) · 롯데면세점 본점(롯데면세점/명동·서울·서울점/LOTTE DUTY FREE 전부 0건) — **3건** |
| 동명이인 · 인접 매장 오매칭 위험 | contentid | areaCode 없이 검색 시 인근 편의점·타지역 동명 장소도 함께 조회됨(예: "인사동 골동품거리"가 경남 진주로도 존재) | 제목 정확 일치 후보 우선 + 서울 주소 포함 후보 우선 선택(자동: `pick_best_match`, 수동: 동일 기준 수기 적용) | 자동 58건 + 수동 5건 전량 적용 |
| 결측치(필수 필드) | contentid / title / addr1 | NULL | 행 제거(`drop_missing_required`) | 0건 (63건 전량 충족) |
| 중복치 | contentid | 서로 다른 후보가 동일 contentid로 매칭 | `drop_duplicates` | 0건 |
| 카테고리 체계 결측·불일치 | raw_categories(데이터랩 "구분") | 자유 텍스트, 장소별 값 개수·표기 상이해 라벨로 부적합 | TourAPI 신 분류체계(lclsSystm1~3)를 공식 category 필드로 전면 대체 | 63건 전량 재태깅 완료, 결측 **0건** |
| **형식 오류(스크립트 결함)** | contentid / contenttypeid / lclsSystm1 | pandas `read_csv` 기본 옵션이 (a) 결측 섞인 ID열을 float로 승격시켜 `127220`→`127220.0`으로 깨뜨리고, (b) 문자열 `"NA"`(자연 코드)를 pandas 기본 결측 사전 값으로 오인해 빈 칸으로 지움(북한산국립공원 1건에서 실제 발생·재현 확인) | `merge_manual_overrides.py`·`preprocess_activities.py`에 `dtype=str, keep_default_na=False, na_filter=False` 적용, 결측 판정도 문자열 `""` 비교로 변경 | 전수(63건) 재현 위험이 있던 결함, 발견 즉시 수정 후 재실행으로 정상값 확인 |
| 스코프 중첩 | lclsSystm1 = FD | 관광지 랭킹에 음식점 1곳(명동교자)이 섞여 들어옴 | 요식업 에이전트와 소속 기준 협의 필요 | **1건** (미해결) |

### 2.2 품질 이슈 진단

| 처리 단계 | 입력 | 출력 | 목적 |
|---|---|---|---|
| 장소명 추출·정규화 | 데이터랩 원본(100행 × 3열) | `popular_places_seoul_en.csv`(66행, english/korean 분리 + raw_categories 병합) | 랭킹 중복행 통합, TourAPI 조회용 표준 명칭 확보 |
| TourAPI 자동 매칭 | 66개 장소명 | `activities_raw.csv`(58건, auto) + `unmatched_for_manual_review.csv`(8건) | 실제 contentid·주소·신 분류체계 확보 |
| 수동 큐레이션 | `unmatched_for_manual_review.csv`(8건) | `activities_raw.csv`에 5건 병합, 3건은 보류 사유와 함께 잔존 | 자동 매칭 실패 건을 사람이 직접 확정(`manual_lookup.py` 조회 → `unmatched_for_manual_review.csv` 수기 기입 → `merge_manual_overrides.py` 병합) |
| 결측·중복 필터링 | `activities_raw.csv`(63건) | `activities_clean.csv`(63건) | 필수 필드 결측 제거(0건), contentid 중복 제거(0건) |
| (예정) 보류 3건 추가 서칭·수기 반영 | `unmatched_for_manual_review.csv`(보류 3건) | `activities_raw.csv`에 추가 병합 예정 | 다른 키워드·자료로 재시도 후 `merge_manual_overrides.py` 재실행 |
| (예정) 내부 카테고리 재태깅 | `activities_clean.csv` + lclsSystm | Tripilot 서비스 category | 추천 로직에 맞는 내부 라벨 체계로 전환 |

### 2.3 전처리 파이프라인 흐름도

```
[데이터랩 원본 100행]
        │  extract_popular_places.py (영/한글 분리, 구분 병합)
        ▼
[popular_places_seoul_en.csv 66건]
        │  tour_api_collect.py (searchKeyword2 + 표기 변형 재시도 + pick_best_match)
        ▼
   ┌─────────────┴─────────────┐
   ▼                           ▼
[activities_raw.csv 58건]   [unmatched_for_manual_review.csv 8건]
   │                           │  manual_lookup.py(후보 조회) → 수기 입력(5건 확정)
   │                           ▼
   │                    merge_manual_overrides.py
   │                           │  (3건은 보류 사유만 기록하고 파일에 잔존 → 추가 서칭 예정)
   └─────────────┬─────────────┘
                  ▼
        preprocess_activities.py (결측/중복 필터링, dtype=str 고정)
                  ▼
        activities_clean.csv (최종 63건)
```

---

## 3. 정제 · 가공 · 분할 절차 및 후속 활용

### 3.1 필드별 처리 기준

- **분할 기준** : 없음 — 액티비티 데이터는 학습용 코퍼스가 아니라 이동 에이전트의 참조 테이블과
  같은 성격의 **추천/조회용 정형 참조 데이터**다. 추천·스코어링 로직이 확정되면 이 기준은 재검토한다.

| 필드 | 타입 | 결측 처리 | 중복 처리 | 이상치 처리 | 비고 |
|---|---|---|---|---|---|
| contentid | STRING(dtype 고정) | 필수 — 없으면 unmatched로 분리 | `drop_duplicates` | - | TourAPI 고유 ID, PK. pandas float 승격 방지를 위해 `dtype=str`로 고정 |
| title | STRING | 필수 — 없으면 unmatched로 분리 | - | - | 장소명(표시용) |
| addr1 | STRING | 필수 — 없으면 unmatched로 분리 | - | - | 주소, 지역 필터링용 |
| english_name / korean_name | STRING | 괄호 파싱 실패 시 korean_name NULL(현재 0건) | groupby로 랭킹 중복 통합 | - | 데이터랩 원표기 |
| lclsSystm1~3 | CATEGORY(dtype 고정) | TourAPI 미제공 시 빈 문자열(현재 매칭 성공 63건은 0건) | - | FD 1건 스코프 중첩(위 2.1) | 대/중/소분류 코드, 공식 category로 채택. `"NA"`(자연) 값이 pandas 기본 결측 사전과 겹쳐 `keep_default_na=False`로 고정 |
| match_type | CATEGORY | - | - | - | auto(58) / manual(5) 구분 |
| raw_categories(구분) | STRING | 있으나 라벨로는 미채택 | - | - | 참고용 원본 랭킹 카테고리, 신뢰도 낮아 폐기 |

### 3.2 후속 단계 활용 적합성 및 재현성 관리

| 구분 | 항목 | 기준 / 결과 | 설명 및 관리 방안 |
|---|---|---|---|
| 분할 | 학습/검증/테스트 | 해당 없음 | 참조형 정형 데이터 — 이동 에이전트와 동일 성격, 추천 로직 확정 시 재검토 |
| 품질 | 결측(매칭 실패, 보류) | 3건 / 66건 (4.5%) | 하모니마트 명동역점 · 현대 아이파크몰 · 롯데면세점 본점 — 이번 라운드 재검색으로도 못 찾음, 영구 제외 아니고 추가 서칭 후 수기 반영 예정 |
| 품질 | 결측(필수 필드) | 0건 | contentid/title/addr1 전량 충족(최종 63건 기준) |
| 품질 | 중복 제거 | 0건 | contentid 기준 |
| 품질 | 카테고리 결측 | 0건 (63건 전량) | raw_categories → lclsSystm 전환으로 해결 |
| 품질 | 스크립트 결함 수정 | 2종(float 승격 · "NA" 결측 오인) | `merge_manual_overrides.py`·`preprocess_activities.py`에 `dtype=str, keep_default_na=False` 적용 후 재실행으로 정합성 확인 |
| 품질 | 민감정보 마스킹 | 해당 없음(0건) | 전부 공공데이터, 개인정보 미수집 |
| 후속 활용 | 추천/필터링 입력 | `activities_clean.csv`(63건, 생성 완료) | 이동·코어 에이전트와 지역/좌표 조인 키 정합 확인 필요 |
| 후속 활용 / 재현성 | 스크립트 재현 | 6개 스크립트, `TOUR_API_KEY` 환경변수 필요 | `.env`는 `.gitignore` 처리(미커밋). 실행 순서: `extract_popular_places.py` → `tour_api_collect.py` → (수기: `manual_lookup.py` → `merge_manual_overrides.py`) → `preprocess_activities.py` |

---

## 4. 변경 이력 및 적용 전략

| 변경일 | 변경자 | 변경내용 | 영향 받는 항목 | 비고 |
|---|---|---|---|---|
| 2026-09-16 | 본인 | 데이터랩 원본 100행 → 고유 장소 66건 추출, TourAPI 자동 매칭(58/66건), raw_categories 결측 문제를 TourAPI 신 분류체계(lclsSystm)로 전면 대체해 해결 | 전체 | 코드/데이터 커밋(`2bf2e82`, `a8b17d9`) |
| 2026-09-17 | 본인 | 데이터 전처리 결과서 1차 작성 — 미매칭 8건 수동 큐레이션 및 `activities_clean.csv` 생성은 미완료 상태로 명시 | 문서 | v1.0-draft |
| 2026-09-17 | 본인 | 미매칭 8건 수동 큐레이션 완료(확정 4·근사매칭 1·미등록 3), `merge_manual_overrides.py`/`preprocess_activities.py`의 pandas 결측 오인·float 승격 결함 발견·수정, `activities_clean.csv` 최초 생성(63건) | 2.1, 2.2, 3.1, 3.2, 스크립트 2종 | v1.1 |
| 2026-09-17 | 본인 | 미등록 3건에 대해 표기 변형 3~5종으로 추가 재검색(하모니마트/아이파크몰/롯데면세점 각각) — 전부 미등록임을 재확인하고 "미등록 확정"으로 종결, `unmatched_for_manual_review.csv` note에 재검색 이력 기록 | 2.2 | v1.2 |
| 2026-09-17 | 본인 | 근사매칭으로 남겨뒀던 "인사동 고미술거리→인사동 문화의 거리"를 담당자가 "고미술거리가 문화의 거리 상권의 하위 구역"이라고 확인해줘 정상 확정으로 정정 — 확정 매칭이 4건에서 5건으로, TODO에서 제거 | 2.2, 3.2 | v1.3 |
| 2026-09-17 | 본인 | 남은 3건을 "미등록 확정(영구 제외)"이 아니라 "보류(추가 서칭 후 수기 반영 예정)"로 재정의, 표지 블록(산출물 단계/제출일자/깃허브 경로/작성 팀원) 추가해 팀 결과서 양식에 맞춤 — 현재 63/66건(95.5%) 커버리지로 1차 제출 | 표지, TODO, 2.1, 2.2, 2.3, 3.2 | v1.4 |
| 2026-09-17 | 본인 | 팀 양식 재확인 결과 이슈유형/대상필드/판정기준/처리방식/처리량 표는 "2.2 품질 이슈 진단"이 아니라 "2.1 데이터 출처 및 현황" 소속이라 2.1로 통합, 기존 "2.3 전처리 파이프라인"을 2.2로 재번호 | 2.1, 2.2 | v1.5 |
| 2026-09-17 | 본인 | v1.5에서 처리단계표와 흐름도를 하나로 합쳤던 걸 되돌림 — 처리단계(처리단계/입력/출력/목적) 표는 "2.2 품질 이슈 진단"으로, 흐름도는 별도 "2.3 전처리 파이프라인 흐름도"로 분리 | 2.2, 2.3 | v1.6 |
